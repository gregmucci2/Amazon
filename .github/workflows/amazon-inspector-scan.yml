name: Amazon Inspector Vulnerability Scan

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * *'  # Daily scan at midnight UTC

permissions:
  contents: read
  security-events: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  scan:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Run Amazon Inspector vulnerability scan
        uses: aws-actions/vulnerability-scan-github-action-for-amazon-inspector@v1.1.2
        with:
          artifact_type: archive
          artifact_path: .
        env:
          AWS_REGION: us-east-1
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      
      - name: List generated files
        run: ls -la inspector* 2>/dev/null || echo "No inspector files found"
      
      - name: Upload scan results to GitHub Security
        if: hashFiles('inspector_scan_*.sarif') != ''
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: inspector_scan_*.sarif
          category: amazon-inspector
